# Define hardware-specific configurations here.
{ config, lib, pkgs, ... }:
{
  # boot.loader.systemd-boot.enable = true;
  # networking.hostName = "my-computer";
  # Add more hardware-specific configurations
}